/// @description  Estado inicial
    
caso = 0;
pixHor = floor(get_render_width() / 125);
pixVer = floor(get_render_height() / 125);
maxMovimientos = floor(random_range(4, 6));
movimientos = 0;
intervalo = 2; // 1.65

alarm[0] = 1;

